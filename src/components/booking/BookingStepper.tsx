import { Check, ChevronRight } from "lucide-react";

const labels = ["Paket", "Lokasi", "Jadwal", "Add-on", "Review", "Bayar"];
export function BookingStepper({ step }: { step: number }) { const progress = ((step + 1) / labels.length) * 100; return <>
  <div className="rounded-[22px] border border-[#D8DEDA] bg-white p-4 shadow-sm md:hidden"><div className="flex items-center justify-between"><span><small className="block text-[9px] font-black tracking-[.14em] text-[#2FA084]">LANGKAH {step + 1} DARI {labels.length}</small><b className="mt-1 flex items-center gap-1 text-sm">{labels[step]}{step < labels.length - 1 && <ChevronRight size={14} className="text-[#9aa4a1]"/>}<span className="font-medium text-[#8a9692]">{labels[step + 1]}</span></b></span><span className="grid size-10 place-items-center rounded-2xl bg-[#FFF6DE] text-xs font-black text-[#2FA084]">{Math.round(progress)}%</span></div><div className="mt-4 h-2 overflow-hidden rounded-full bg-[#edf1ef]"><span className="block h-full rounded-full bg-gradient-to-r from-[#93bf35] to-[#FFE394] transition-[width] duration-500 ease-out" style={{width:`${progress}%`}}/></div></div>
  <div className="hidden rounded-[24px] border border-[#D8DEDA] bg-white px-6 py-4 shadow-sm md:block"><div className="flex items-center">{labels.map((label, i) => <div key={label} className="flex flex-1 items-center last:flex-none"><div className="flex flex-col items-center gap-1.5"><span className={`grid size-9 place-items-center rounded-full text-xs font-black transition-all duration-500 ${i < step ? "bg-[#FFE394] text-[#2FA084]" : i === step ? "scale-110 bg-[#2FA084] text-white shadow-[0_0_0_5px_#FFF6DE]" : "bg-[#edf1ef] text-[#8a9692]"}`}>{i < step ? <Check size={15} strokeWidth={3}/> : i+1}</span><span className={`text-[10px] font-extrabold transition-colors ${i === step ? "text-[#2FA084]" : "text-[#8a9692]"}`}>{label}</span></div>{i < labels.length-1 && <span className="relative mx-3 mb-5 h-1 flex-1 overflow-hidden rounded-full bg-[#D8DEDA]"><span className={`absolute inset-y-0 left-0 rounded-full bg-[#b6df4f] transition-all duration-500 ${i < step ? "w-full" : "w-0"}`}/></span>}</div>)}</div></div>
</>; }
