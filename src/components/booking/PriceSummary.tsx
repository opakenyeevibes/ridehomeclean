import { ChevronUp, ReceiptText, ShieldCheck } from "lucide-react";
import { formatPrice } from "@/lib/utils";
export function PriceSummary({ packagePrice, addOnPrice, urgentFee = 0, crewFee = 0, platformFee = 5000, compact = false }: { packagePrice: number; addOnPrice: number; urgentFee?: number; crewFee?: number; platformFee?: number; compact?: boolean }) { const total = packagePrice + addOnPrice + urgentFee + crewFee + platformFee; if (compact) return <div className="flex items-center justify-between gap-3"><span><small className="flex items-center gap-1 text-[9px] font-bold text-[#72807b]"><ReceiptText size={11}/> TOTAL ESTIMASI</small><b className="mt-0.5 block text-lg tracking-tight text-[#2FA084]">{formatPrice(total)}</b></span><span className="flex items-center gap-1 rounded-xl bg-[#FFF6DE] px-3 py-2 text-[10px] font-extrabold text-[#2FA084]">Lihat rincian <ChevronUp size={13}/></span></div>; return <div className="relative overflow-hidden rounded-[28px] bg-[#2FA084] p-5 text-white shadow-[0_18px_45px_rgba(31,111,95,.18)]"><div className="absolute -right-10 -top-10 size-32 rounded-full bg-[#FFE394]/10"/><div className="relative"><div className="mb-5 flex items-center justify-between"><span className="flex items-center gap-2"><span className="grid size-9 place-items-center rounded-xl bg-white/10 text-[#FFE394]"><ReceiptText size={17}/></span><span><h3 className="text-sm font-extrabold">Ringkasan harga</h3><small className="text-[9px] text-white/45">Diperbarui otomatis</small></span></span><span className="rounded-full bg-[#FFE394]/12 px-2 py-1 text-[9px] font-bold text-[#FFE394]">TRANSPARAN</span></div><div className="space-y-3.5 text-xs text-white/60"><div className="flex justify-between"><span>Paket layanan</span><b className="text-white">{formatPrice(packagePrice)}</b></div><div className="flex justify-between"><span>Layanan tambahan</span><b className="text-white">{formatPrice(addOnPrice)}</b></div>{urgentFee > 0 && <div className="flex justify-between"><span>Prioritas jadwal</span><b className="text-white">{formatPrice(urgentFee)}</b></div>}{crewFee > 0 && <div className="flex justify-between"><span>Tambahan tim</span><b className="text-white">{formatPrice(crewFee)}</b></div>}<div className="flex justify-between"><span>Biaya platform</span><b className="text-white">{formatPrice(platformFee)}</b></div></div><div className="my-4 border-t border-dashed border-white/20"/><div className="flex items-end justify-between"><span className="text-xs font-semibold">Total estimasi</span><b className="text-xl tracking-tight text-[#FFE394]">{formatPrice(total)}</b></div><p className="mt-4 flex gap-2 rounded-2xl bg-white/7 p-3 text-[9px] leading-4 text-white/55"><ShieldCheck size={14} className="shrink-0 text-[#FFE394]"/>Harga transparan dan terlindungi jaminan layanan. Tidak ada biaya tersembunyi.</p></div></div>; }
